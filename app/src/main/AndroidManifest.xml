<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.netodology.androidappbar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ru.netology.androidappbar.CalendarActivity"></activity>
        <activity android:name="ru.netology.androidappbar.AddressActivity" />
        <activity android:name="ru.netology.androidappbar.PayActivity" />
        <activity android:name="ru.netology.androidappbar.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ru.netology.androidappbar.NotesActivity" />
    </application>

</manifest>